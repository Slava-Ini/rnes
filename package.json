{
  "name": "rnes",
  "version": "0.0.1",
  "description": "react native event source",
  "main": "index.js",
  "scripts": {
    "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo",
    "build": "npm run clean && tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Slava-Ini",
  "license": "MIT",
  "devDependencies": {
    "rimraf": "^4.1.3",
    "typescript": "^4.9.5"
  }
}
